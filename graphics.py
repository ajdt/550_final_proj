#!/usr/bin/env python
import pygame

# TODO: make this a singleton!!!
class GraphicsManager:
	def __init__(self):
		# initialize pygame code
		self.scale # scale to draw grid at
		self.g_height, self.g_width # height and width of grid

	# helper function to initialize pygame
	def initPygame():
	
	# draws unit
	def drawBot(position):

	# draw grid based on global view
	def drawGrid(global_view):

	# update the screen, called after all draw 
	# functions to render changes to the screen
	def updateScreen():
		
